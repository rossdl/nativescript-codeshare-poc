{
    "files.exclude": {
        "**/*.js": { "when": "$(basename).ts" },
        "**/*.css": { "when": "$(basename).scss" },
    },
    "workbench.colorCustomizations": {
        "activityBar.background": "#271db4"
    }
}